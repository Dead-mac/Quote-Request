

import React from 'react';
import { InsuranceType } from '../types';
import { ArrowRightIcon } from './icons/ArrowRightIcon';

interface InsuranceTypeSelectorProps {
  types: InsuranceType[];
  onSelect: (type: InsuranceType) => void;
}

const InsuranceTypeSelector: React.FC<InsuranceTypeSelectorProps> = ({ types, onSelect }) => {
  return (
    <div className="animate-fade-in">
      <h2 className="text-2xl sm:text-3xl font-bold text-center text-zinc-800 mb-2">What type of insurance do you need?</h2>
      <p className="text-center text-zinc-600 mb-8 sm:mb-12">Select one to get started.</p>
      <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4 sm:gap-6">
        {types.map((type) => (
          <button
            key={type.id}
            onClick={() => onSelect(type)}
            className="group text-left p-5 bg-white rounded-xl border border-zinc-200 hover:border-teal-500 hover:shadow-lg transition-all duration-300 ease-in-out transform hover:-translate-y-1"
          >
            <div className="flex items-center justify-center w-12 h-12 rounded-full bg-zinc-100/80 shadow-sm mb-4 group-hover:bg-teal-500 transition-colors duration-300">
              <type.Icon className="w-6 h-6 text-teal-600 group-hover:text-white transition-colors duration-300" />
            </div>
            <h3 className="font-bold text-zinc-800 text-lg mb-1">{type.name}</h3>
            <p className="text-sm text-zinc-500 mb-4">{type.description}</p>
            <div className="flex items-center text-teal-600 font-semibold text-sm">
              <span>Select</span>
              <ArrowRightIcon className="w-4 h-4 ml-1 transform transition-transform duration-300 group-hover:translate-x-1" />
            </div>
          </button>
        ))}
      </div>
    </div>
  );
};

export default InsuranceTypeSelector;
